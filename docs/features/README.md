# Feature specifications (legacy location)

**New and in-flight specs live under [`specs/`](../../specs/).** Process: [`specs/README.md`](../../specs/README.md). Templates: [`specs/_templates/`](../../specs/_templates/). Index: [`specs/STATUS.md`](../../specs/STATUS.md).

Files in this directory that have been migrated are **stubs** pointing at `specs/features/NNN-slug/`.

[`complete/`](complete/) and [`archive/`](archive/) remain until a later mechanical move into `specs/complete/` and `specs/archive/`. They are still authoritative for shipped or archived behavior until that move.

Do not add new feature specs here.
