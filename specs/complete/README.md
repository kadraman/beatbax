# Complete specifications

Shipped features. Still authoritative for implemented behavior.

Each folder is `NNN-slug/` with `spec.md` only (plan/tasks are not required for historical completions). New work is completed by moving folders from [`specs/features/`](../features/) here.

Index: [STATUS.md](../STATUS.md).

Do not load this entire tree into every agent turn. Open only the complete specs linked from the active feature or from STATUS.md.

Legacy stubs remain under [`docs/features/complete/`](../../docs/features/complete/) for old links.
