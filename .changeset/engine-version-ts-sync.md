---
"@beatbax/engine": patch
---

Keep engine plugin metadata version accurate by syncing `src/version.ts` from `packages/engine/package.json` via the repo `sync-versions` workflow run during `version-packages`.
