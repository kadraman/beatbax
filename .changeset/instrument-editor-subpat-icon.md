---
"@beatbax/desktop": patch
---

Show the Game Boy subpattern name with a go-to-source icon.

The Instrument Editor Voice row no longer hides `subpat=` behind a cramped empty field and a “Show in editor” button. The icon jumps to and highlights the `subpat` definition, not the selected `inst` line. Macro lock copy is “Software macros are locked while subpat is set.”
