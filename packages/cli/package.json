{
  "name": "@beatbax/cli",
  "version": "0.2.0",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "beatbax": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "node --enable-source-maps src/cli.js",
    "pretest": "npm run build --if-present",
    "test": "jest --config ./jest.config.cjs --passWithNoTests"
  },
  "dependencies": {
    "@beatbax/engine": "file:../engine",
    "commander": "^10.0.0"
  }
}
