{
  "name": "@beatbax/web-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "prepare-engine": "node scripts/prepare-engine.js",
    "dev": "npm run prepare-engine && vite --force",
    "dev:clean": "npm run prepare-engine && vite --force",
    "build": "npm run prepare-engine && vite build",
    "test": "jest",
    "test:watch": "jest --watch",
    "preview": "vite preview"
  },
  "dependencies": {
    "@beatbax/engine": "^0.8.0",
    "@beatbax/plugin-chip-nes": "^0.1.0",
    "@tailwindcss/vite": "^4.2.2",
    "buffer": "^6.0.3",
    "dompurify": "^3.3.3",
    "marked": "^5.1.2",
    "monaco-editor": "^0.45.0",
    "nanostores": "^1.2.0",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "@types/dompurify": "^3.0.5",
    "@types/jest": "^29.5.0",
    "@types/marked": "^5.0.2",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^30.2.0",
    "ts-jest": "^29.1.0",
    "vite": "^5.0.0"
  }
}
